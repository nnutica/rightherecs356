<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Righthere_Demo.Views.DiaryHistoryPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="DiaryHistoryPage">
    <ContentPage.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
            <GradientStop Offset="0.1" Color="#FFEEF2" />
            <GradientStop Offset="1.0" Color="#D0E8FF" />
        </LinearGradientBrush>
    </ContentPage.Background>

    <VerticalStackLayout Padding="20">
        <Label
            FontAttributes="Bold"
            FontSize="24"
            HorizontalOptions="Center"
            Text="📘 ประวัติไดอารี่" />

        <CollectionView
            x:Name="DiaryListView"
            SelectionChanged="DiaryListView_SelectionChanged"
            SelectionMode="Single">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame
                        Margin="0,5"
                        Padding="10"
                        BackgroundColor="White"
                        CornerRadius="12"
                        HasShadow="True">
                        <VerticalStackLayout>
                            <Label
                                FontAttributes="Bold"
                                Text="{Binding CreatedAt, StringFormat='วันที่: {0:dd MMM yyyy}'}"
                                TextColor="DarkSlateBlue" />
                            <Label
                                FontSize="14"
                                Text="{Binding Content}"
                                TextColor="Gray" />
                        </VerticalStackLayout>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>
</ContentPage>